# Spec: Priority PCD review task

- Decision
    - NFS compliant
        - Case type → Are you CPS Direct? (CPSD)
            - Yes → Check answers
            - No → Transfer case
                - No → Check answers
                - Yes → Area → Unit → Check answers
    - NFS non-compliant
        - Case type → NFS reasons → CPSD → Action plan date → Check answers
    - Priority / Red rejection
        - Case type → Priority reasons → CPSD → Action plan date → Check answers
