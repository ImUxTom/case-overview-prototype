{% set filterOptionsHtml %}

  {{ appCheckboxFilter({
    idPrefix: "taskListFilters[owner]",
    name: "taskListFilters[owner]",
    classes: "govuk-checkboxes--small owner-filter",
    attributes: { id: 'owner-filter' },
    fieldset: { legend: { text: "Owner", classes: "govuk-fieldset__legend--s" } },
    maxHeight: true,
    items: ownerItems,
    values: selectedOwnerFilters,
    selectedItems: selectedOwnerItems
  }) }}

  {{ govukCheckboxes({
    idPrefix: "taskListFilters[units]",
    name: "taskListFilters[units]",
    classes: "govuk-checkboxes--small",
    fieldset: {
      legend: {
        text: "Unit",
        classes: "govuk-fieldset__legend--s"
      }
    },
    items: unitItems,
    values: selectedUnitFilters
  }) }}

  {{ govukCheckboxes({
    idPrefix: "taskListFilters[urgent]",
    name: "taskListFilters[urgent]",
    classes: "govuk-checkboxes--small",
    fieldset: {
      legend: {
        text: "Urgent",
        classes: "govuk-fieldset__legend--s"
      }
    },
    items: urgentItems,
    values: selectedUrgentFilters
  }) }}

  {{ govukCheckboxes({
    idPrefix: "taskListFilters[reminder]",
    name: "taskListFilters[reminder]",
    classes: "govuk-checkboxes--small",
    fieldset: {
      legend: {
        text: "Reminder",
        classes: "govuk-fieldset__legend--s"
      }
    },
    items: reminderItems,
    values: selectedReminderFilters
  }) }}

  {{ govukCheckboxes({
    idPrefix: "taskListFilters[timeLimitType]",
    name: "taskListFilters[timeLimitType]",
    classes: "govuk-checkboxes--small",
    fieldset: {
      legend: {
        text: "Time limit",
        classes: "govuk-fieldset__legend--s"
      }
    },
    items: timeLimitTypeItems,
    values: selectedTimeLimitTypeFilters
  }) if 1==2}}

  {{ govukCheckboxes({
    idPrefix: "taskListFilters[paceClockRange]",
    name: "taskListFilters[paceClockRange]",
    classes: "govuk-checkboxes--small",
    fieldset: {
      legend: {
        text: "PACE clock",
        classes: "govuk-fieldset__legend--s"
      }
    },
    items: paceClockRangeItems,
    values: selectedPaceClockRangeFilters
  }) }}

  {{ govukCheckboxes({
    idPrefix: "taskListFilters[custodyTimeLimitRange]",
    name: "taskListFilters[custodyTimeLimitRange]",
    classes: "govuk-checkboxes--small",
    fieldset: {
      legend: {
        text: "Custody time limit",
        classes: "govuk-fieldset__legend--s"
      }
    },
    items: custodyTimeLimitRangeItems,
    values: selectedCustodyTimeLimitRangeFilters
  }) }}

  {{ govukCheckboxes({
    idPrefix: "taskListFilters[statutoryTimeLimitRange]",
    name: "taskListFilters[statutoryTimeLimitRange]",
    classes: "govuk-checkboxes--small",
    fieldset: {
      legend: {
        text: "Statutory time limit",
        classes: "govuk-fieldset__legend--s"
      }
    },
    items: statutoryTimeLimitRangeItems,
    values: selectedStatutoryTimeLimitRangeFilters
  }) }}

  {{ govukCheckboxes({
    idPrefix: "taskListFilters[hearingDateRange]",
    name: "taskListFilters[hearingDateRange]",
    classes: "govuk-checkboxes--small",
    fieldset: {
      legend: {
        text: "Hearing date",
        classes: "govuk-fieldset__legend--s"
      }
    },
    items: hearingDateRangeItems,
    values: selectedHearingDateRangeFilters
  }) }}

  {{ govukCheckboxes({
    idPrefix: "taskListFilters[severities]",
    name: "taskListFilters[severities]",
    classes: "govuk-checkboxes--small",
    fieldset: {
      legend: {
        text: "Due",
        classes: "govuk-fieldset__legend--s"
      }
    },
    items: severityItems,
    values: selectedSeverityFilters
  }) }}

  {{ appCheckboxFilter({
    idPrefix: "taskListFilters[taskNames]",
    name: "taskListFilters[taskNames]",
    classes: "govuk-checkboxes--small task-name-filter",
    attributes: { id: 'task-name-filter' },
    fieldset: { legend: { text: "Task", classes: "govuk-fieldset__legend--s" } },
    maxHeight: true,
    items: taskNameItems,
    values: selectedTaskNameFilters,
    selectedItems: selectedTaskNameItems
  }) }}

{% endset %}

<section aria-labelledby="app-filter-heading">
<div class="app-filter">
  <div class="app-filter__header">
    <div class="app-filter__header-title">
      <h2 class="govuk-heading-m" id="app-filter-heading">Filter</h2>
    </div>
    <div class="app-filter__header-action">

    </div>
  </div>
  <div class="app-filter__content">
    {% if selectedFilters.categories.length %}
      <div class="app-filter__selected">
        <div class="app-filter__selected-heading">
          <div class="app-filter__heading-title">
            <h3 class="govuk-heading-m govuk-!-margin-bottom-1">Selected filters</h3>
            <p class="govuk-body"><a class="govuk-link govuk-link--no-visited-state" href="/tasks/clear-filters">Clear filters</a></p>
          </div>
        </div>
        {% for category in selectedFilters.categories %}
          <h3 class="govuk-heading-s govuk-!-margin-bottom-0">{{ category.heading.text }}</h3>
          <ul class="app-filter-tags">
            {% for item in category.items %}
              <li><a class="app-filter__tag" href="{{item.href}}"><span class="govuk-visually-hidden">Remove this filter</span> {{item.text}}</a></li>
            {% endfor %}
          </ul>
        {% endfor %}
      </div>
    {% endif %}
    <div class="app-filter__options">
      <form method="get" novalidate>
        <button class="govuk-button" data-module="govuk-button">
          Apply filters
        </button>
        {{filterOptionsHtml | safe}}
      </form>
    </div>
  </div>
</div>
</section>
