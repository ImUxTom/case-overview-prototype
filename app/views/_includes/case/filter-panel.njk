{% set filterOptionsHtml %}

  {{ appCheckboxFilter({
    idPrefix: "caseListFilters[prosecutors]",
    name: "caseListFilters[prosecutors]",
    classes: "govuk-checkboxes--small prosecutor-filter" ,
    attributes: {
      id: 'prosecutor-filter'
    },
    fieldset: {
      legend: {
        text: "Prosecutor",
        classes: "govuk-fieldset__legend--s"
      }
    },
    maxHeight: true,
    items: prosecutorItems,
    values: selectedProsecutorFilters,
    selectedItems: selectedProsecutorItems
  }) }}

  {{ appCheckboxFilter({
    idPrefix: "caseListFilters[paralegalOfficers]",
    name: "caseListFilters[paralegalOfficers]",
    classes: "govuk-checkboxes--small paralegal-officer-filter" ,
    attributes: {
      id: 'paralegal-officer-filter'
    },
    fieldset: {
      legend: {
        text: "Paralegal officer",
        classes: "govuk-fieldset__legend--s"
      }
    },
    maxHeight: true,
    items: paralegalOfficerItems,
    values: selectedParalegalOfficerFilters,
    selectedItems: selectedParalegalOfficerItems
  }) }}

  {{ govukCheckboxes({
    idPrefix: "caseListFilters[unit]",
    name: "caseListFilters[unit]",
    classes: "govuk-checkboxes--small",
    fieldset: {
      legend: {
        text: "Unit",
        classes: "govuk-fieldset__legend--s"
      }
    },
    items: unitItems,
    values: data.caseListFilters.unit
  }) if unitItems.length > 1}}

  {{ govukCheckboxes({
    idPrefix: "caseListFilters[policeUnit]",
    name: "caseListFilters[policeUnit]",
    classes: "govuk-checkboxes--small",
    fieldset: {
      legend: {
        text: "Police unit",
        classes: "govuk-fieldset__legend--s"
      }
    },
    items: policeUnitItems,
    values: data.caseListFilters.policeUnit
  }) if policeUnitItems.length }}

  {# {{ govukCheckboxes({
    idPrefix: "caseListFilters[complexities]",
    name: "caseListFilters[complexities]",
    classes: "govuk-checkboxes--small",
    fieldset: {
      legend: {
        text: "Complexity",
        classes: "govuk-fieldset__legend--s"
      }
    },
    items: complexityItems,
    values: data.caseListFilters.complexities
  }) }} #}

  {% if data.user.role == "Manager" %}
    {{ govukCheckboxes({
      idPrefix: "caseListFilters[dga]",
      name: "caseListFilters[dga]",
      classes: "govuk-checkboxes--small",
      fieldset: {
        legend: {
          text: "DGA",
          classes: "govuk-fieldset__legend--s"
        }
      },
      items: dgaItems,
      values: data.caseListFilters.dga
    }) }}
  {% endif %}

{% endset %}

<section aria-labelledby="app-filter-heading">
<div class="app-filter">
  <div class="app-filter__header">
    <div class="app-filter__header-title">
      <h2 class="govuk-heading-m" id="app-filter-heading">Filter</h2>
    </div>
    <div class="app-filter__header-action">

    </div>
  </div>
  <div class="app-filter__content">
    {% if selectedFilters.categories.length %}
      <div class="app-filter__selected">
        <div class="app-filter__selected-heading">
          <div class="app-filter__heading-title">
            <h3 class="govuk-heading-m govuk-!-margin-bottom-1">Selected filters</h3>
            <p class="govuk-body"><a class="govuk-link govuk-link--no-visited-state" href="/cases/clear-filters">Clear filters</a></p>
          </div>
        </div>
        {% for category in selectedFilters.categories %}
          <h3 class="govuk-heading-s govuk-!-margin-bottom-0">{{ category.heading.text }}</h3>
          <ul class="app-filter-tags">
            {% for item in category.items %}
              <li><a class="app-filter__tag" href="{{item.href}}"><span class="govuk-visually-hidden">Remove this filter</span> {{item.text}}</a></li>
            {% endfor %}
          </ul>
        {% endfor %}
      </div>
    {% endif %}
    <div class="app-filter__options">
      <form method="get" novalidate>
        <button class="govuk-button" data-module="govuk-button">
          Apply filters
        </button>
        {{filterOptionsHtml | safe}}
      </form>
    </div>
  </div>
</div>
</section>
